{% extends 'base.html.twig' %}

{% block title %}Usager index{% endblock %}

{% block body %}

    {% if(usager != null) %}
        <h1 class="text-2xl border-b border-slate-700">Bienvenue {{ usager.prenom }} {{ usager.nom }}.</h1>
    {% else %}
        <h1 class="text-2xl border-b border-slate-700 text-red-500">Erreur : usager non trouver.</h1>
    {% endif %}
{% endblock %}
